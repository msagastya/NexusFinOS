// This file is the entry point for initializing the sync engine within the mobile app.
import { startDiscovery } from '@sync/index';

console.log('Sync init loaded');

// Example of how the discovery could be started.
// In a real app, this would be tied to a user action or app state.
// startDiscovery('my-vault-id', 'My-Phone', (peer) => {
//   console.log('Discovered peer:', peer);
// });
