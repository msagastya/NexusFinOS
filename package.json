{
  "name": "nexusfinos",
  "private": true,
  "version": "1.0.0",
  "description": "NEXUS FinOS",
  "main": "index.js",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:all": "npm run build --workspace=packages/shared-domain && npm run build --workspace=packages/finance-engine && npm run build --workspace=packages/sync-engine && npm run build --workspace=packages/ingestion-engine && npm run build --workspace=packages/upi-engine",
    "check": "npm run build:all && npx tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:mobile": "npm run start --workspace=apps/mobile"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}